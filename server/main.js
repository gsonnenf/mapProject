import { Meteor } from 'meteor/meteor';
//import { ImportPortals } from '/server/ImportPortals.js';

Meteor.startup(() => {
  // code to run on server at startup
  //ImportPortals.Import(); Will import initial portal list
});

